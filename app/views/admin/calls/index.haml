%table.table
  %thead
    %th
      Тип
    %th
      Имя
    %th
      Телефон
    %th
      Дата заявки
    %th
      Статус СМС
    %th
      Actions
  %tbody
    - @calls.each do |call|
      %tr
        %td
          = call.call_type
        %td
          = link_to call.username, admin_call_path(call)
        %td
          = call.userphone
        %td
          = Russian::strftime(call.created_at, '%d %B в %H:%M')
        %td
          = call.sms_status
        %td
          = link_to admin_call_path(call), method: :delete do
            %i.fas.fa-times
