- @items.each do |item|
  %p{style: 'float: left; margin: 10px;'}
    %br
    = image_tag item.img.url(:small)
    = link_to item.name, admin_item_path(item)
    %br
    Минимальная партия:
    = item.min_order
    = item.quantity_type
= will_paginate @items

