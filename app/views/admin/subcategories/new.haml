.container
  = form_for [:admin, @subcategory] do |f|
    .form-group
      = f.label :name, 'Название'
      = f.text_field :name, class: 'form-control'
    .form-group
      = f.submit 'Создать коллекцию', class: 'btn btn-info'
