.container
  = form_for [:admin, @texture] do |f|
    .form-group
      = f.label :name, 'Текстура'
      = f.text_field :name, class: 'form-control'
    .form-group
      = f.submit 'Создать текстуру', class: 'btn btn-info'
