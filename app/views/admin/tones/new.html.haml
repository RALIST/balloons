.container
  = form_for [:admin, @tone] do |f|
    .form-group
      = f.label :name, 'Оттенок'
      = f.text_field :name, class: 'form-control'
    .form-group
      = f.submit 'Создать оттенок', class: 'btn btn-info'
