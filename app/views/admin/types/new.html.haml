.container
  = form_for [:admin, @type] do |f|
    .form-group
      = f.label :name, 'Оттенок'
      = f.text_field :name, class: 'form-control'
    .form-group
      = f.submit 'Создать тип', class: 'btn btn-info'
