%span.title
  В композицию входит:
  %br
- @position = @cart.positions.find_by(composition_id: c.id)
- @position.subpositions.order(:id).each do |s|
  %ul
    %li
      = s.item.name
      = link_to delivery_add_quantity_path(id: s.id), method: :post, remote: true do
        %i.fa.fa-plus
      = s.quantity
      = link_to delivery_down_quantity_path(id: s.id), method: :post, remote: true do
        %i.fa.fa-minus
      %br
      
%span.add-to-comp{style: 'cursor: pointer'} Добавить в композицию
.form-add-sub
  = render partial: 'delivery/carts/add', locals: {position: @position}