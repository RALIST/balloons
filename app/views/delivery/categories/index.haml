- breadcrumb :categories
.container
  %h1.h2.font-weight-bold.text-center
    Коллекции воздушных шаров по тематике
    .small.text-muted
      Воздушные шары с персонажами любимых мультфильмов на любой праздник
  %hr
  .card-columns
    - @categories.each do |category|
      - cache category do
        = link_to category_path(category) do
          .card
            .card-body
              .row.align-items-center
                .col-5
                  = image_tag category.image.img(:small), title: category.name, alt: category.name, data: {toggle: 'tooltip'}
                .col-7.lead
                  = category.name.capitalize
