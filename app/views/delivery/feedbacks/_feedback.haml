#feedback.my-3
  .feed-img.text-center
    = image_tag feedback.img(:preview), class: 'rounded-circle'
  .feed-text.text-center
    .lead
      = feedback.name
    %span
      = feedback.body
    .small
      = Russian::strftime(feedback.created_at, "%d %b  '%y")


