- breadcrumb :feedbacks
- unless @feedbacks.any?
  %h2.text-center.text-secondary.my-5.border-bot
    Пока нет ни одного отзыва!
-else
  .container.border-bot
    %h2.text-center.my-5
      Отзывы наших клиентов
    .row
      = render @feedbacks, cache: true
    %ul.feed.list-unstyled.d-flex.justify-content-between.align-items-center.mt-4
      = will_paginate @feedbacks,  container: false, next_label: 'Вперед <i class="fa fa-angle-right" aria-hidden="true"></i>', previous_label: '<i class="fa fa-angle-left" aria-hidden="true"></i> Назад'
.container
  = render 'form'
