.container-fluid
  .container
    %h2.text-center Оформление воздушными шарами #{params[:title].capitalize}
    .row.justify-content-around.card-deck
      = render partial: 'thumb_composition', collection: @compositions, as: :c
    .row.text-center
      = will_paginate @compositions

= render 'delivery/main/modal_composition'
