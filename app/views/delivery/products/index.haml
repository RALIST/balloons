- breadcrumb :products
.container-fluid.bg-grad.py-3
  %h1.h2.text-center.font-weight-bold.mb-3.text-white
    Гелиевые шары с доставкой в #{t("cities.#{@city}.where")}
    .small
      Большой ассортимент воздушных шаров на любой праздник
  .row.text-center
    #offer-icon.text-white
      %i.fas.fa-thumbs-up.fa-3x.my-1
      .h4.text-white
        Проклеиваем все шары
        .small
          Гарантируем 3 дня полета или заменим брак!
    #offer-icon.text-white
      %i.fas.fa-car.fa-3x.my-1
      .h4.text-white
        Круглосуточная доставка
        .small
          Доставим ваш заказ точно в срок!
    #offer-icon.text-white
      %i.fas.fa-birthday-cake.fa-3x.my-1
      .h4.text-white
        Индивидуальный дизайн
        .small
          Подберем уникальное оформление под ваш праздник!
  .text-center
    .lead.text-white
      Помочь с выбором?
    = button_tag 'Оставить заявку', class: 'btn btn-block btn-info col-lg-6 m-auto', data:{ toggle: 'modal', target: '#call-modal'}
.container.mt-4
  %h2.mt-5.text-center=link_to 'Воздушные шары по коллекциям',categories_path
  %hr
  .row
    .col-12.my-3
      .input-group
        = text_field_tag :search,'', class: 'form-control', placeholder: 'Поиск по категориям'
        %span.input-group-append
          = button_tag(class: 'btn btn-info', id: 'clear-search', title: 'Очистить', data: {toggle: 'tooltip'}) do
            %i.fa.fa-times
    - @categories.each do |category|
      - cache category do
        .col-6.col-lg-3.my-2
          = link_to category_path(category) do
            %span.as_btn{data: {toggle: 'tooltip'}, title: "#{image_tag category.products.first.img(:small)}"}
              = category.name.capitalize
              (#{category.products.count})
  %h2.mt-5.text-center=link_to 'Воздушные шары по цветам', colors_path
  %hr
  .row
    - @colors.each do |color|
      - unless color.name == 'ассорти'
        = link_to color_path(color) do
          - cache color do
            .col-6.col-lg-3.my-2
              .as_btn= color.name.capitalize




