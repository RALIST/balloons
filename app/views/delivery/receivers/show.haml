- breadcrumb @person
.container-fluid.c-list
  %h1.h5.text-center
    .small
      Воздушные шары
    %span.text-uppercase
      = @person.title.capitalize
  .row.justify-content-around.compositions
    = render partial: 'delivery/main/thumb_composition', collection: @compositions, as: :c
  .row.text-center#pagy
    = raw pagy_nav(@pagy)