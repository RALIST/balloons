- breadcrumb @person
.container-fluid.c-list
  %h1.h5.text-center
    .small
      Воздушные шары
    %span.text-uppercase
      = @person.title.capitalize
  = render 'delivery/compositions/compositions_list'