$('#add-sub<%= @position.id %>').on('ajax:success', function(){
  $(this).closest('.info').find('.subpositions').first().html('<%= j(render @position.subpositions) %>');
  $('.total').html(<%= @cart.total_price %>)
});
