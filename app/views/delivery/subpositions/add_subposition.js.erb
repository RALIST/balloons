<% @cart.compositions.each do |c| %>
  $('#add-sub').submit(function(event){
    event.preventDefault();
    $(this).closest('.info').first().html(.html('<%= j(render partial: "delivery/carts/subposition", locals: {c: c} ) %>'));
  });
<% end %>
