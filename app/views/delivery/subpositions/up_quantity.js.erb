<% @position = @subposition.position %>
$('.sub-<%= @subposition.id %>').on('ajax:success', function(){
  $(this).closest('.info').find('.subpositions').first().html('<%= j(render @position.subpositions.order(:id)) %>');
  $('.total').html(<%= @cart.total_price %>);
});
