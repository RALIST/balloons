- breadcrumb :tags
.container-fluid.c-list
  %h1.h5.text-center
    %span.text-uppercase
      Воздушные шары на любой праздник
  .row.compositions
    = render partial: 'delivery/main/thumb_composition', collection:  @compositions, cached: true, as: :c
  .row.text-center#pagy
    = raw pagy_nav(@pagy)