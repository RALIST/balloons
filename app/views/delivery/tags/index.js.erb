$('.compositions').append('<%= j(render partial: "delivery/main/thumb_composition", collection: @compositions, as: :c) %>');

$('.small-compositions').append('<%= j(render partial: "delivery/compositions/small_composition", collection: @compositions, as: :composition) %>');

$('#pagy').html('<%= j(raw pagy_nav(@pagy)) %>');
if ($('.pagination .next a').length < 1 ){
  $(window).off('scroll');
  $('#pagy').remove();
};
